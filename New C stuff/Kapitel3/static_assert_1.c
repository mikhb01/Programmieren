// Kapitel3/static_assert_1.c

#include <stdio.h>
#include <assert.h>

int main(void){

    // static_assert( sizeof(long double) == 16,
    //                 "Need 16 byte long double" );

    return 0;
}