#include <stdio.h>
#include <stdbool.h>

int main(){
    // variable =   Allocated space in memory o store a value.
    //              We refer to a variable's name to access the stored value.
    //              That variable now behaves as if it was the value it contains.
    //              But we need to declare what type of data we are storing

    // int x; //declaration
    // x = 123; //initialization
    // int y = 321; //declaration + initialization

    // int age = 2147483547; //integer | 4 bytes (-2.147.483.648 to 2.147.483.547) %d
    // unsigned int k = 4294967295; // 4 bytes (0 to +4.294.967.295) %u
    // float gpa = 3.141592; //floating point number | 4 bytes (32 bits of precision) 6-7 digits %f
    // double d = 3.141592653589793; //long float | 8 bytes (64 bits of precision) 15-16 digits %lf
    // char grade = 'C'; //single character %c
    // char name[] = "Bro"; //array of characters %s

    // bool e = true; //1 byte (true or false) %d

    // char f = 120; //1 byte (-128 to +127) %d or %c
    // unsigned char g = 255; //1 byte (0 to 255) %d or %c

    // short int h = 32767; //2 bytes (-32.768 to +32.767) %d
    // unsigned short int i = 65535; //2 bytes (0 to +65.535) %d

    // long long int l = 9223372036854775807; //8 bytes (-9 quintillion to + 9quintillion) %lld
    // unsigned long long int m = 18446744073709551615U; //8 bytes (0 to +18 quintillion) %llu

    // printf("Hello %s\n", name);
    // printf("You are %d years old\n", age);
    // printf("Your average grade is %c\n", grade);
    // printf("Your gpa is %f\n", gpa);
    // printf("%0.10lf", d); 
    // printf("%d",e);
    // printf("%d",f);
    // printf("%c",e); //ASCII
    // printf("%c",g);
    // printf("%d\n",h);
    // printf("%d\n",i);
    // printf("%d\n",age);
    // printf("%u\n",k);
    // printf("%lld\n",l);
    // printf("%llu\n",m);


    return 0;
}